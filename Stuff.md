# Some Stuff That Needs Doing

- We need to make sure that all our internal functions work well when editing like our own files in the XDG config areas, so any macros or whatever what we've written, should tab-complete for us.
- No list would be complete without Sharks.
- We should probably have something that lets us live edit our settings/configs, perhaps even a modern gui looking monster.
- Spell-checking (prose), DAP debugging client
- Multibuffers (stitched excerpts from multiple files)
- VCS-agnostic plugin system (git/hg/svn/jj via Janet plugins)
- Multiple cursors  deferred, has a real design writeup already in the roadmap
- Built-in terminal panel, task runner, remote SSH editing
- Per-file session persistence (remember point/scroll per file)
- Minimap, richer theme set
- Companion tooling: setup wizard, tree-sitter-assisted formatter
