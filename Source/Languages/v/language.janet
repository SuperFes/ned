# .v belongs to Verilog (far more files in the wild); a V source file
# needs a manual mode switch unless it is a .vsh script or v.mod.
{:name "v"
 :extensions [".vsh" ".vv"]
 :filenames ["v.mod"]
 :line-comment "//"
 :lsp-root-markers ["v.mod"]
}
