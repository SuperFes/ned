#; Symbol-kind query. A section is the one thing worth a breadcrumb in a unit
#; file, and it reads as a namespace over its directives -- same call as ini.

(section
  (section_header
    (section_name) @name)) @definition.module
